# Point this to your Render backend URL in production, e.g.:
# VITE_API_URL=https://student-readiness-backend.onrender.com
# Leave empty for local development (Vite dev proxy forwards /api to localhost:5000)
VITE_API_URL=
